{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$ref": "#/definitions/TimeConfigurationSpec",
    "definitions": {
        "TimeConfigurationSpec": {
            "properties": {
                "node_selector": {
                    "$ref": "#/definitions/api.system.v1.NodeSelector",
                    "additionalProperties": true,
                    "description": "节点选择器"
                },
                "timezone": {
                    "type": "string",
                    "description": "时区设置（如 Asia/Shanghai）"
                },
                "ntp": {
                    "$ref": "#/definitions/api.system.v1.NtpConfig",
                    "additionalProperties": true,
                    "description": "NTP 配置"
                }
            },
            "additionalProperties": true,
            "type": "object",
            "title": "时间配置相关定义",
            "description": "时间配置相关定义  时间配置项"
        },
        "api.system.v1.NodeSelector": {
            "properties": {
                "machine_id": {
                    "type": "string",
                    "description": "机器ID匹配"
                }
            },
            "additionalProperties": true,
            "type": "object",
            "title": "Node Selector",
            "description": "节点选择器"
        },
        "api.system.v1.NtpConfig": {
            "properties": {
                "enable": {
                    "type": "boolean",
                    "description": "是否启用 NTP"
                },
                "servers": {
                    "items": {
                        "type": "string"
                    },
                    "type": "array",
                    "description": "NTP 服务器列表"
                }
            },
            "additionalProperties": true,
            "type": "object",
            "title": "Ntp Config",
            "description": "NTP 配置"
        }
    }
}