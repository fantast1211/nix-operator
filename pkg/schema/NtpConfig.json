{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$ref": "#/definitions/NtpConfig",
    "definitions": {
        "NtpConfig": {
            "properties": {
                "enable": {
                    "type": "boolean",
                    "description": "是否启用 NTP"
                },
                "servers": {
                    "items": {
                        "type": "string"
                    },
                    "type": "array",
                    "description": "NTP 服务器列表"
                }
            },
            "additionalProperties": true,
            "type": "object",
            "title": "Ntp Config",
            "description": "NTP 配置"
        }
    }
}